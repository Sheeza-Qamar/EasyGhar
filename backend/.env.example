# Easyghar Backend — Environment
# Copy this file to .env and set your values. Do not commit .env.

DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/easyghar?ssl-mode=REQUIRED
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME

PORT=5000
FRONTEND_URL=http://localhost:3000

# Admin dashboard (Bearer token for /api/admin)
ADMIN_API_KEY=your-secret-admin-key

# JWT for worker/customer auth (login response token)
JWT_SECRET=your-jwt-secret-change-in-production
